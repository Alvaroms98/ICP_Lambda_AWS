{
  "name": "lambda_post",
  "version": "1.0.0",
  "description": "Funcion lambda de AWS para recibir los POST del frontend",
  "main": "lambda.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1074.0"
  }
}
